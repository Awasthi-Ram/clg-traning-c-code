//WAP to check if a number is positive or negative using if-else						
#include<stdio.h>
int main(void)
{
    int num;
    printf("enter any no.");
    scanf("%d",&num);
    if(num>=0)
    {
        printf("the gaven no. is positive");
    }
    else
    {
        printf("the gaven no is negative");
    }
}