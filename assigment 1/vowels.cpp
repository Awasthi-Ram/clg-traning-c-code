//#include<stdio.h>
#include<iostream>
int main(void)
{
    char i;
    //scanf("%c",&i);
    cout<<i<<"\n";
    switch(i)
    {
        case 'a': printf("the gaven no. is vowel");
        break;
        case 'e': printf("the gaven no. is vowel");
        break;
        case 'i': printf("the gaven no. is vowel");
        break;
        case 'o': printf("the gaven no. is vowel");
        break;
        case 'u': printf("the gaven no. is vowel");
        break;
        
        case 'A': printf("the gaven no. is vowel");
        break;
        case 'E': printf("the gaven no. is vowel");
        break;
        case 'I': printf("the gaven no. is vowel");
        break;
        case 'O': printf("the gaven no. is vowel");
        break;
        case 'U': printf("the gaven no. is vowel");
        break;
        default
        :printf("the gaven no. is not vowel");
    }
}